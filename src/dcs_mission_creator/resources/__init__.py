"""Package resource root.

Holds the rendered map overlay (`caucasus_overlay.jpg`) and the per-theater
overlay artifacts under `overlays/<theater>/`. The build cache lives under
`_build_cache/` and is gitignored.
"""
